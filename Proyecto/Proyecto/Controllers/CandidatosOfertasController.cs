using Proyecto.Models;
using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.Linq;
using System.Web;
using System.Web.Mvc;

namespace Proyecto.Controllers
{
    public class CandidatosOfertasController : Controller
    {
        //curriculumModel model = new curriculumModel();
        //// GET: CandidatosOfertas
        //public ActionResult Index()
        //{
        //    return View();
        //}

        ////POST: CandidatoOfertas
        //[HttpPost]
        //public ActionResult Create(candidatos_ofertas canof)
        //{
        //    usuarios user = new usuarios();
        //    user = (usuarios)Session["usuario"];
        //    Debug.WriteLine(user.id_usuario);
        //    int userid = user.id_usuario;
        //    curriculum cv = new curriculum();
        //    cv = model.getCurriculum(userid);
        //    Debug.WriteLine(cv.id_curriculum);
        //    int id = cv.id_curriculum;
        //    return View();
        //}
    }
}