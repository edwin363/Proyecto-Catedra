@model IEnumerable<Proyecto.Models.candidatos_ofertas>

@{
    ViewBag.Title = "candiToAp";
}

<h2>candiToAp</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_cand_ofer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ofertas.nombre_puesto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_curriculum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_cand_ofer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ofertas.nombre_puesto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_curriculum)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Ver cv", "Details", new { id=item.id_curriculum }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })|
        </td>
    </tr>
}

</table>
